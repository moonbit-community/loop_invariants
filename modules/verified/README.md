# Verified Examples

This module contains small proof-oriented examples for binary search, integer
square root, division, upper bound, and non-negative counting.

Packages are organized as one verified example per directory. Each package keeps
its MoonBit source and any tests beside its generated interface file.

Run from the workspace root:

```bash
moon check
moon test
moon info
moon fmt
```
