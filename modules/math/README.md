# Math Algorithms

This module contains number theory, transforms, recurrence, basis, polynomial,
and linear-algebra oriented packages.

Packages are organized as one algorithm or closely related group per directory.
Each package contains a `README.mbt.md` tutorial with examples that are checked
by `moon test`.

Run from the workspace root:

```bash
moon check
moon test
moon info
moon fmt
```
